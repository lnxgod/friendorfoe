; Friend or Foe — Uplink Firmware (ESP32-C3)
; WiFi STA + HTTP upload to backend

[platformio]
src_dir = main

[env:uplink]
platform = espressif32
board = esp32-c3-devkitm-1
framework = espidf
monitor_speed = 921600
board_build.partitions = partitions.csv
build_flags =
    -DUPLINK_BOARD
